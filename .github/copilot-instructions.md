# Copilot Instructions

## Project Guidelines
- Use `Calisto MT` instead of `Segoe UI` for UI font styling in this project.
- The logo icon for the login screen should be a red blood-drop style glyph in a rounded-corner badge.